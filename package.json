{
  "name": "leattrace-portal",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "install:all": "npm --prefix frontend install",
    "dev": "npm --prefix frontend run dev"
  }
}
